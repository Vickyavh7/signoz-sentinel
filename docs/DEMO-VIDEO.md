# Demo video — recording notes

Keep under **3 minutes**. Show real UI, not slides.

## Script

1. SigNoz healthy + demo services visible  
2. `NS=foundry ./demo/break.sh errors`  
3. Alert fires → webhook → Slack Incident Sentinel report (root cause + evidence links)  
4. Auto-generated postmortem (`GET /postmortems/{ruleId}` or Slack follow-up)  
5. SigNoz: investigation trace for service `incident-sentinel`  
6. Dashboards: **Copilot Operations** + **Agent Quality**  
7. Flash `evals/RESULTS.md` (measured accuracy)  
8. Closer: cost meta-alert — the copilot watching its own spend  

Upload unlisted YouTube (or similar) and paste the URL into the WeMakeDevs form.
